/* tslint:disable:no-unused-variable */
import {async, ComponentFixture, TestBed} from '@angular/core/testing';

import {RepresentativeProductDetailComponent} from './representativeProduct-detail.component';

describe('RepresentativeProductDetailComponent', () => {
  let component: RepresentativeProductDetailComponent;
  let fixture: ComponentFixture<RepresentativeProductDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [RepresentativeProductDetailComponent]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RepresentativeProductDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  //Tests

});
